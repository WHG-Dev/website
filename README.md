
  # Design based on image

  This is a code bundle for Design based on image. The original project is available at https://www.figma.com/design/exDgnzhHqCS0hpMKHVriuq/Design-based-on-image.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  